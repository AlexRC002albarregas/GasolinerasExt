import { Routes } from '@angular/router';
import { GasolinerasComponent } from './domains/components/gasolineras/gasolineras.component';

export const routes: Routes = [

    { path: '', component: GasolinerasComponent },

];
